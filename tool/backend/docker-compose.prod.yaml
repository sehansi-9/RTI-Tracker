# Production docker-compose.prod.yaml
# Use with: docker compose -f docker-compose.yaml -f docker-compose.prod.yaml up -d
services:
  # fastapi server
  server:
    restart: always
    # Prod command (no reload, optimized for performance)


